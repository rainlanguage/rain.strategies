# Stub crate that replaces raindex_bindings via [patch].
# The real crate requires Solidity build artifacts (rainix-sol-prelude),
# but raindex_app_settings never actually imports it.
[package]
name = "raindex_bindings"
version = "0.0.0-alpha.0"
edition = "2021"
publish = false
